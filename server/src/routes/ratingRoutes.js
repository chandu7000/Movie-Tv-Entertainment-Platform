const express=require('express'); const {requireAuth}=require('../middleware/auth'); const {upsertRating,getMyRating,removeRating}=require('../controllers/ratingController'); const router=express.Router(); router.use(requireAuth); router.post('/',upsertRating); router.put('/',upsertRating); router.get('/:mediaType/:tmdbId',getMyRating); router.delete('/:mediaType/:tmdbId',removeRating); module.exports=router;
